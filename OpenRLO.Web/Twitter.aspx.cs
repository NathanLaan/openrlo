using System;
using System.Web;
using System.Web.UI;
using System.Web.UI.WebControls;
//using TweetSharp.Twitter.Model;
//using TweetSharp.Twitter.Service;

namespace OpenRLO.Web
{
  public partial class Twitter : System.Web.UI.Page
  {
    protected void Page_Load(object sender, EventArgs e)
    {


      //// Instantiate a service
      //TwitterService service = new TwitterService();
      //service.AuthenticateAs("nathanlaan", "deacoN.2009");
      

      //// Obtain objects
      //IEnumerable<TwitterStatus> userTweets = service.ListTweetsOnUserTimeline(10);

      //this.tweetList.DataSource = userTweets;
      //this.tweetList.DataBind();
    }
  }
}